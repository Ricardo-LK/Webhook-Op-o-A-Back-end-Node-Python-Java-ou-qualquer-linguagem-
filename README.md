# Webhook-Op-o-A-Back-end-Node-Python-Java-ou-qualquer-linguagem-

Testei a aplicação rodando o servidor localmente com npm start.
Depois enviei uma requisição POST usando curl para a rota /webhook com um JSON contendo id e mensagem.
Verifiquei que a API retornou status de sucesso e salvou a mensagem na lista em memória.
Em seguida, acessei a rota GET /mensagens para listar todas as mensagens recebidas.
